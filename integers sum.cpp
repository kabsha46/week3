#include<iostream>
using namespace sttd;

main( )
{ 
  

   int num , sum =0;

   cout << " enter 5 integers "<<endl;
  
  cin >> num;
   sum = sum + num;
  
   cin >> num;
   sum = sum + num;
  
  cin >> num;
  sum = sum + num;

   cin >> num;
   sum = sum + num;

   cin >> num;
   sum = sum + num;
 
   cout << " the sum is  " << sum <<endl;


}